import "./Footer.scss"
import {FC} from "react";

interface Props {
    hoverSrc:string,
    src:string,
}

export const Icon: FC<Props> = ({}) => {

    return (
        <div className={""}>

        </div>
    )
}
